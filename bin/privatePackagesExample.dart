import 'privateClasses.dart';

void exampleMethod(){
 final x = Example(1,2);

 //can't even be accesses from the same file
  // x._private;

  //can't even be accesses from the same file
  // x._privateMethod();
  // can't even be accesses from the same file
  // x._privateGetter;


} 